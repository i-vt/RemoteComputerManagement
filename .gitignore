# ── Rust build artifacts ───────────────────────────────────────────────
/target/
**/*.rs.bk
*.pdb

# ── Compiled agent binaries ────────────────────────────────────────────
/dist/exe_*
/dist/dll_*
/dist/service_*
/dist/stager_*
/dist/*.exe
# Keep server_keys.json out of git — contains private signing keys
/dist/server_keys.json

# ── TLS certificates and keys ──────────────────────────────────────────
# Generated at runtime by start_docker.sh — never commit private keys
/certs/ca.key
/certs/ca.srl
/certs/server.key
/certs/server.key.der
/certs/server.csr
/certs/client.key
/certs/client.key.der
/certs/client.csr
# Public certs are also generated at runtime, but you may choose to commit
# ca.crt if you want to pin it in agents built from source. Uncomment to exclude:
# /certs/ca.crt
# /certs/server.crt
# /certs/client.crt

# ── Runtime database ───────────────────────────────────────────────────
# Contains operator credentials, session data, build keys, audit log
c2_audit.db
c2_audit.db-shm
c2_audit.db-wal

# ── Operator data ──────────────────────────────────────────────────────
/downloads/
/data/
/logs/
/history.txt

# ── Docker ─────────────────────────────────────────────────────────────
# Ignore local override files but keep the main compose file
docker-compose.override.yml
docker-compose.local.yml

# ── IDE and editor ─────────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── Environment and secrets ────────────────────────────────────────────
.env
.env.local
.env.*.local
secrets/

# ── Test artifacts ─────────────────────────────────────────────────────
/tests/docker/tmp/
*.test.log

# ── Summary / generated docs ───────────────────────────────────────────
summary.txt
